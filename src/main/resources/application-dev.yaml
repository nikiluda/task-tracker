spring:
  datasource:
    url: jdbc:h2:mem:task_tracker
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  secret: my-super-secret-key-for-task-tracker-dev-2026 #${JWT_SECRET}
  expiration: 86400000